# Customer Required Info

为了把当前源码从可运行 Demo 接到真实设备/真实后台，请客户补充以下资料。2026-06-14 客户已补充一部分，状态如下：

1. OpenAPI
   - 已收到测试环境 baseUrl：`https://solar.siseli.com/openapis`
   - 已收到 AppID
   - 已收到 AppSecret（源码不写死，构建时注入）
   - 已收到测试账号：`jason1324`
   - 已确认 OpenAPI 账号校验接口返回“账号已存在”
   - 已确认 `POST /login/account` 需要传 `MD5(password)`，按该规则登录成功并返回 token
   - 已确认近端 `POST /near/dtu/checkin` 可用，并返回协议号 `ED7504199`、版本 `2`
   - 已确认客户解绑后可添加设备；`POST /device/add/single` 已把 DTUID `30340387838800344455` 绑定到 `jason1324`
   - 已获得真实设备 ID：`488330252727058433`
   - 仍需客户确认：设备何时开始上报实时数据；当前实时状态和能量流接口返回 `No latest data`
   - 已确认：当前设备不支持 `peakValley` 削峰填谷接口，Smart Schedule 只能保留 UI/本地配置或等待支持该能力的设备
   - 正式环境 baseUrl 如已有也请一并提供

2. 接口 JSON 示例
   - 已通过 Swagger 确认：登录、设备列表、单设备实时状态、能量流、告警列表等接口路径
   - 仍建议客户提供真实返回 JSON，便于把字段精确映射到 UI
   - 电池电量、输入功率、输出功率、太阳能输入
   - 历史数据：日、周、月、自定义时间段
   - 通知列表
   - 修改设备名称
   - 删除设备
   - Smart Schedule 读取/保存
   - 睡眠模式读取/保存
   - 电池优先级读取/保存
   - 当前测试设备的属性 key / 配置 key 对照表，尤其是电池 SOC、AC 输入、Solar 输入、输出功率、Sleep Mode、电池优先级

3. 设备资料
   - 已收到 Sierro 1000 / Sierro2000 型号参数
   - 已收到设备序列号示例：`SN26102503Z6104955`
   - 已收到 DTUID 示例
   - 已收到固件版本 / 硬件版本
   - 已收到容量、最大输入功率、最大输出功率、电池类型、电压、频率

4. 二维码
   - 设备二维码扫码后的真实内容
   - 字段含义：DTUID、SN、MAC、model、token 或 URL
   - 1-2 个真实二维码样例

5. 蓝牙配网
   - 已按客户 PDF 实现基础 BLE 协议层
   - 仍需真机确认 BLE 名称前缀是否固定为 SSL_
   - 仍需真机确认是否开启蓝牙密码、默认蓝牙密码或验证规则
   - 仍需真机确认配网是否只需 SSID / Key
   - 仍需真机确认配网成功后的判断命令或接口
   - 至少一台真机用于联调

6. 数据口径
   - 电池剩余时间计算公式
   - CO2 Reduced 计算系数
   - 温度单位：F 或 C
   - 图表时间粒度和字段单位

7. UI
   - 如需 1:1 还原，请提供 Figma 在线链接或每个页面导出的 PNG。

说明：本次交付不包含上架、Apple/Google 证书、开发者账号、商店隐私材料。
